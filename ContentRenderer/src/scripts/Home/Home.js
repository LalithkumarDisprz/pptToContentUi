import styles from './Home.module.scss';

// import ContentView from '../../view/ContentView';

function Home() {
  return (
    <div className={`${styles.template}`}>
      {/* <ContentView data={sampleJson} /> */}
    </div>
  );
}

Home.propTypes = {};

export default Home;
